# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
dist/
out/
.turbo/
target/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Rust
*.pdb
debug/
release/

# Godot
.godot/
*.import

# Test/coverage
coverage/
*.lcov

# Flywheel / Agent Mail local setup
.agent-mail-store/
mcp_agent_mail/
.mcp.json
codex.mcp.json
cline.mcp.json
windsurf.mcp.json
.claude/settings.local.json
.codex/hooks/

# bv (beads viewer) local config and caches
.bv/

# beads recovery backups (SQLite WAL/SHM snapshots)
.beads/.br_recovery/

# Claude Code worktrees (agent-spawned isolated checkouts)
.claude/worktrees/

# Orchestrator / swarm runtime state (logs, locks, assignments, local DBs).
# Regenerated every run — never meant to be committed.
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
.codex/orchestrator/
.codex/orchestrator-notices.log
.codex/bead.db
.codex/bead.db-shm
.codex/bead.db-wal
.claude/*.lock
