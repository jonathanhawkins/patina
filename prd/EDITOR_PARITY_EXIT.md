# Editor Parity — Exit Criteria

One unchecked checkbox per acceptance test referenced across the per-lane execution maps (`prd/EDITOR_PARITY_EXECUTION_MAP.md`). The planner ticks a box when its named test passes; the phase exits when every box is checked.

> **A criterion is satisfied only when an APP-LEVEL test passes** — one that boots the real editor (`EditorServerHandle::start` / `examples/editor.rs`) and drives it over HTTP the way a user does. A lib unit test that only exercises an isolated model struct (e.g. `MainScreenSwitcher`, `SceneWorkspace`, `InspectorPanel`) is **insufficient**: it can pass while the feature is never wired into the running editor — which is exactly how the main-screen mode switch, New Scene, and selection→inspector all "passed" while being broken in the app. Every user-visible criterion MUST have a test that asserts the assembled app behaves, not merely that a model method returns the right value. See beads labeled `planner-methodology` (e.g. the per-mode e2e smoke test) for the gate that enforces this.

## Now

- [x] `scene-tree-ops-add-child-node` Add Child Node action inserts a new node of the chosen type as a child of the selected node and selects it (test: `scene_tree_add_child_node`)
- [x] `scene-tree-ops-instance-child-scene` Instance Child Scene action embeds an external scene as an instanced child with the instance badge (test: `scene_tree_instance_child_scene`)
- [x] `scene-tree-ops-rename-node` Rename via double-click / F2 updates the node name with collision-safe uniquification among siblings (test: `scene_tree_rename_node_unique`)
- [x] `scene-tree-ops-delete-node` Delete (Del) removes the selected node(s) and their subtrees, with multi-selection support (test: `scene_tree_delete_node_subtree`)
- [x] `scene-tree-ops-duplicate-node` Duplicate (Ctrl+D) clones the selected node and its subtree as a sibling with a uniquified name (test: `scene_tree_duplicate_node`)
- [x] `scene-tree-ops-reparent-node` Reparent (drag-drop or Reparent dialog) moves a node under a new parent while preserving its subtree (test: `scene_tree_reparent_node`)
- [x] `scene-tree-ops-reorder-siblings` Move Up / Move Down reorders a node among its siblings (test: `scene_tree_reorder_siblings`)
- [x] `scene-tree-ops-cut-copy-paste` Cut / Copy / Paste node clipboard operations move or clone subtrees across the hierarchy (test: `scene_tree_cut_copy_paste_node`)
- [x] `scene-tree-ops-change-type` Change Type converts a node to a compatible type, preserving children and shared properties (test: `scene_tree_change_node_type`)
- [x] `scene-tree-ops-editable-children` Editable Children / Make Local toggles ownership and exposes instanced subtree nodes for editing (test: `scene_tree_editable_children_make_local`)
- [x] `scene-tree-ops-group-membership` Context-menu group editing adds and removes the selected node from named groups (test: `scene_tree_group_membership`)
- [x] `scene-tree-visibility-toggle` Render and toggle the per-node visibility (eye) indicator for CanvasItem and Node3D rows, persisting the `visible` property and propagating to descendants (test: `scene_tree_visibility_toggle_reflects_state`)
- [x] `scene-tree-lock-indicator` Render the lock indicator and support locking/unlocking a node so it cannot be picked in the viewport, with the lock badge shown on the row (test: `scene_tree_lock_indicator_blocks_viewport_pick`)
- [x] `scene-tree-group-children-indicator` Render the "group" (children-selection-lock) indicator and prevent child selection in the viewport when a parent is grouped (test: `scene_tree_group_indicator_locks_child_selection`)
- [x] `scene-tree-script-badge` Render the script-attached badge with the correct script icon and open the script in the editor when activated (test: `scene_tree_script_badge_opens_script`)
- [x] `scene-tree-instance-badge` Render the scene-instance badge for instanced scenes and open the instanced scene when the badge is activated (test: `scene_tree_instance_badge_opens_source_scene`)
- [x] `scene-tree-signal-connection-indicator` Render the signal-connection indicator when a node has at least one connected signal, updating live as connections are added or removed (test: `scene_tree_signal_indicator_tracks_connections`)
- [x] `scene-tree-unique-name-indicator` Render the unique-name-in-owner (`%`) indicator and keep it consistent with the node's `unique_name_in_owner` flag, surfacing collisions (test: `scene_tree_unique_name_indicator_reflects_flag`)
- [x] `scene-tree-configuration-warning` Render the per-node configuration-warning triangle when `Node._get_configuration_warnings` returns messages and expose the warning text on hover/activation (test: `scene_tree_configuration_warning_surfaces_messages`)
- [x] `scene-tree-selection-state-sync` Keep multi-selection highlighting in the dock synchronized with viewport and inspector selection, including range/additive selection semantics (test: `scene_tree_selection_state_stays_synced`)
- [x] `inspector-object-header` Render the inspected-object header with class icon, object/resource name, and class label matching Godot's EditorInspector header row. (test: `inspector_object_header_renders_class_and_name`)
- [x] `inspector-history-back-forward` Implement back/forward navigation buttons that move through the stack of previously inspected objects, enabling/disabling at the ends of the stack. (test: `inspector_history_back_forward_navigates_stack`)
- [x] `inspector-history-dropdown` Add the history dropdown menu listing recently inspected objects, selecting an entry re-inspects that object. (test: `inspector_history_dropdown_lists_and_selects`)
- [x] `inspector-resource-toolbar-actions` Add the resource action toolbar (edit, load, clear, save-as, copy, paste) wired to the inspected Resource, disabling actions that do not apply to the current object. (test: `inspector_resource_toolbar_actions_apply`)
- [x] `inspector-subresource-breadcrumb` Render a sub-resource breadcrumb so editing an embedded resource shows the path back to the owning object and clicking a crumb navigates up. (test: `inspector_subresource_breadcrumb_navigates_up`)
- [x] `inspector-pin-lock` Implement the pin/lock toggle that keeps the current object inspected even as the scene-tree selection changes. (test: `inspector_pin_lock_holds_selection`)
- [x] `inspector-copy-paste-resource` Implement copy/paste of a resource reference across the resource toolbar so a copied resource can be pasted into a compatible property slot. (test: `inspector_copy_paste_resource_round_trip`)
- [x] `inspector-history-persists-per-scene` Persist the inspected-object history per open scene so switching scene tabs restores that scene's navigation stack. (test: `inspector_history_persists_per_scene`)
- [x] `inspector-props-typed-editors` Property editors render and commit per type (numeric, string, bool, enum, Vector2/3, Color, NodePath, resource) (test: `inspector_typed_property_editors`)
- [x] `inspector-props-drag-to-adjust` Numeric fields support click-drag scrubbing to adjust the value with step granularity (test: `inspector_numeric_drag_to_adjust`)
- [x] `inspector-props-inline-expression` Numeric fields accept inline math expressions that evaluate on commit (test: `inspector_numeric_inline_expression`)
- [x] `inspector-props-revert-default` A revert button appears when a property differs from its default and resets it on click (test: `inspector_revert_to_default`)
- [x] `inspector-props-linked-components` Vector/Rect editors offer a proportional lock so editing one component scales the others by ratio (test: `inspector_linked_proportional_components`)
- [x] `inspector-props-copy-paste-value` Copy/Paste property value transfers a value between compatible properties (test: `inspector_copy_paste_property_value`)
- [x] `inspector-props-copy-property-path` Copy Property Path yields the scripting path for the selected property (test: `inspector_copy_property_path`)
- [x] `inspector-props-multi-edit` Editing a property with multiple objects selected applies the change to all of them (test: `inspector_multi_node_property_edit`)
- [x] `inspector-props-key-animation` A key affordance lets a property value be inserted as an animation track keyframe from the inspector (test: `inspector_key_property_to_animation`)
- [x] `inspector-props-undo-redo-dirty` Property edits push a reversible undo/redo entry and mark the scene dirty (test: `inspector_property_undo_redo_dirty`)
- [x] `inspector-property-categories` Render class-based category separators (with class icon and name) that group inherited properties by the class that declares them, matching Godot's EditorInspector category rows. (test: `inspector_property_categories_group_by_class`)
- [x] `inspector-export-groups` Implement `@export_group` / `@export_subgroup` collapsing so prefixed properties nest under collapsible group and subgroup headers. (test: `inspector_export_groups_nest_and_collapse`)
- [x] `inspector-export-hint-widgets` Map `PropertyHint` values from `@export` annotations to the correct editor widget (range slider, enum dropdown, file/dir picker, multiline text, flags). (test: `inspector_export_hints_select_widget`)
- [x] `inspector-subresource-inline-edit` Allow a resource-typed property to expand inline so its sub-properties are editable without leaving the parent object. (test: `inspector_subresource_inline_edit_persists`)
- [x] `inspector-favorites-pinning` Implement property favorites so a user can pin properties to a Favorites section that stays at the top across objects of the same class. (test: `inspector_favorites_pin_to_top`)
- [x] `inspector-export-category-separator` Support `@export_category` to insert a labeled category divider in script-declared property order. (test: `inspector_export_category_divider_renders`)
- [x] `inspector-typed-collection-export` Render typed array and dictionary exports with add/remove/reorder controls and per-element editors driven by the element type hint. (test: `inspector_typed_collection_export_edits`)
- [x] `inspector-usage-flags-visibility` Honor property usage flags (storage, editor, read-only, no-editor) so script and engine properties show, hide, or lock in the inspector accordingly. (test: `inspector_usage_flags_control_visibility`)
- [x] `viewport-select-click` Click-select picks the topmost node under the cursor and updates the selection (test: `viewport_select_click`)
- [x] `viewport-select-box` Rubber-band box selection selects all nodes intersecting the dragged rectangle (test: `viewport_select_box`)
- [x] `viewport-select-overlap-cycle` Repeated click / modifier cycles selection through stacked overlapping nodes (test: `viewport_select_overlap_cycle`)
- [x] `viewport-pan` The viewport can be panned via middle-drag or space-drag (test: `viewport_pan`)
- [x] `viewport-zoom` Wheel zoom and zoom controls scale the view about the cursor with reset-to-100% (test: `viewport_zoom`)
- [x] `viewport-frame-selection` Frame-selection centers and fits the selected node(s) in the viewport (test: `viewport_frame_selection`)
- [x] `viewport-locked-node-behavior` Locked and grouped nodes follow Godot selection rules in the viewport (test: `viewport_locked_grouped_selection`)
- [x] `viewport-mode-toolbar` The viewport toolbar exposes select/pan/ruler modes and a zoom indicator (test: `viewport_mode_toolbar`)
- [x] `viewport-gizmo-move` Move gizmo drags the selected node's position along axis handles or freely from the center (test: `viewport_gizmo_move`)
- [x] `viewport-gizmo-rotate` Rotate gizmo rotates the selection around its pivot by dragging the rotation handle (test: `viewport_gizmo_rotate`)
- [x] `viewport-gizmo-scale` Scale gizmo scales the selection via axis and uniform handles (test: `viewport_gizmo_scale`)
- [x] `viewport-gizmo-origin-marker` The origin/pivot marker renders at the node's transform origin and can be moved to set the pivot (test: `viewport_gizmo_origin_marker`)
- [x] `viewport-gizmo-pivot-relative` Rotate and scale operate about the current pivot, not the node center, when a custom pivot is set (test: `viewport_gizmo_pivot_relative_transform`)
- [x] `viewport-gizmo-local-global-toggle` A local/global toggle switches gizmo handle orientation between the node's local axes and world axes (test: `viewport_gizmo_local_global_toggle`)
- [x] `viewport-gizmo-snap-during-drag` Gizmo drags honor active translate/rotate/scale snap settings (test: `viewport_gizmo_snap_during_drag`)
- [x] `viewport-gizmo-multi-node-and-undo` Gizmo transforms apply to all selected nodes about a shared pivot and push a single undo entry (test: `viewport_gizmo_multi_node_undo`)
- [x] `viewport-grid-display` Draw the configurable 2D grid (step, offset, primary-line interval) and toggle its visibility from the View menu. (test: `viewport_grid_display_respects_step_offset`)
- [x] `viewport-grid-snapping` Snap dragged/created nodes to the grid when snap is enabled, honoring the grid step and offset. (test: `viewport_grid_snapping_quantizes_position`)
- [x] `viewport-rulers` Render horizontal and vertical rulers showing canvas coordinates that track zoom and pan. (test: `viewport_rulers_track_zoom_and_pan`)
- [x] `viewport-guides` Support dragging horizontal and vertical guides out of the rulers, moving them, and snapping nodes to them. (test: `viewport_guides_create_move_and_snap`)
- [x] `viewport-smart-snap` Implement smart/relative snapping to other nodes' edges, centers, and anchors with snap-line feedback. (test: `viewport_smart_snap_aligns_to_siblings`)
- [x] `viewport-origin-axes` Draw the world origin axes (X/Y reference lines) and toggle them from the View menu. (test: `viewport_origin_axes_render_at_zero`)
- [x] `viewport-snap-config` Provide the snap configuration dialog for grid step/offset, rotation snap step, and scale snap step, persisting values across sessions. (test: `viewport_snap_config_persists_and_applies`)
- [x] `viewport-canvas-debug-overlays` Render toggleable canvas overlays for the visibility rect, navigation polygons, and y-sort ordering preview. (test: `viewport_canvas_debug_overlays_toggle`)
- [x] `top-bar-scene-tabs` Open scenes appear as tabs in the top bar and clicking a tab makes it the active scene (test: `top_bar_scene_tabs_switch`)
- [x] `top-bar-tab-unsaved-and-close` Tabs show an unsaved-changes marker and a close affordance with a save prompt when dirty (test: `top_bar_tab_unsaved_and_close`)
- [x] `top-bar-tab-context-menu` Right-clicking a scene tab offers close/close-others/close-all and path actions (test: `top_bar_tab_context_menu`)
- [x] `top-bar-tab-reorder` Scene tabs can be reordered by dragging (test: `top_bar_tab_reorder`)
- [x] `top-bar-open-new-scene` The new-scene control adds a fresh untitled scene tab and focuses it (test: `top_bar_open_new_scene`)
- [x] `top-bar-run-controls` Run controls play the project, play the current scene, pause, and stop (test: `top_bar_run_controls`)
- [x] `top-bar-play-custom-scene` A play-custom-scene control runs a chosen scene other than the main scene (test: `top_bar_play_custom_scene`)
- [x] `top-bar-mode-switch` The main-screen mode switcher toggles between 2D, 3D, Script, and AssetLib views (test: `top_bar_editor_mode_switch`)
- [x] `menus-scene-actions` Implement the Scene menu (New Scene, New Inherited Scene, Open Scene, Save, Save As, Save All, Close Scene, Revert Scene, Quit) wired to the scene-document lifecycle. (test: `menus_scene_actions_dispatch`)
- [x] `menus-project-actions` Implement the Project menu (Project Settings, Version Control, Export, Reload Current Project, Quit to Project List) wired to the corresponding editor subsystems. (test: `menus_project_actions_open_subsystems`)
- [x] `menus-debug-toggles` Implement the Debug menu toggles (Visible Collision Shapes, Visible Navigation, Visible Paths, Synchronize Scene Changes, Synchronize Script Changes) as persisted checkable items applied to play sessions. (test: `menus_debug_toggles_persist_and_apply`)
- [x] `menus-help-actions` Implement the Help menu (Search Help, Online Docs, Report a Bug, About) routing to the help search dialog, external links, and the about dialog. (test: `menus_help_actions_route_targets`)
- [x] `menus-editor-actions` Implement the Editor menu (Editor Settings, Command Palette, Editor Layout, Toggle Fullscreen, Manage Editor Features) wired to editor-level commands. (test: `menus_editor_actions_dispatch`)
- [x] `menus-open-recent` Implement Scene > Open Recent with a maintained most-recent-first list that opens the selected scene and persists across sessions. (test: `menus_open_recent_tracks_and_opens`)
- [x] `menus-action-shortcuts` Bind menu actions to their keyboard shortcuts and display the accelerator text in the menu item label. (test: `menus_action_shortcuts_bind_and_display`)
- [x] `menus-action-enablement` Enable/disable and check/uncheck menu items based on editor state (e.g. Save disabled with no unsaved changes, Revert disabled for an unsaved scene). (test: `menus_action_enablement_reflects_state`)
- [x] `create-node-type-tree` The dialog shows the node-type inheritance tree rooted at the requested base type (test: `create_node_type_tree`)
- [x] `create-node-incremental-search` Typing in the search box incrementally filters the tree and highlights matches (test: `create_node_incremental_search`)
- [x] `create-node-best-match-confirm` Enter confirms the best-matching type and double-click / Create button instantiates the selected type (test: `create_node_best_match_confirm`)
- [x] `create-node-favorites` Types can be favorited and appear in a Favorites section at the top of the dialog (test: `create_node_favorites`)
- [x] `create-node-recent` Recently created types appear in a Recent section ordered by last use (test: `create_node_recent`)
- [x] `create-node-description-panel` Selecting a type shows its description/help text in the dialog (test: `create_node_description_panel`)
- [x] `create-node-child-of-selection` The created node is inserted as a child of the currently selected scene node (or as root when empty) (test: `create_node_child_of_selection`)
- [x] `create-node-2d-catalog` The dialog exposes the common 2D node catalog and helper nodes (test: `create_node_2d_catalog`)
- [x] `bottom-panel-bar` Implement the bottom panel bar that lists registered panels (Output, Debugger, Audio, Shader, …) and shows/hides the active one, with an expand-to-fill toggle. (test: `bottom_panel_bar_toggles_active_panel`)
- [x] `bottom-output-console` Implement the Output console capturing game/editor stdout, errors, and warnings with clear, and per-severity filtering. (test: `bottom_output_console_captures_and_filters`)
- [x] `bottom-debugger-stack-vars` Implement the Debugger stack-frame list and variable inspector that populate on a breakpoint/error and let the user step and inspect locals. (test: `bottom_debugger_stack_and_variables_populate`)
- [x] `bottom-debugger-errors-tab` Implement the Debugger Errors tab listing runtime errors/warnings with message, source, and a jump-to-source action. (test: `bottom_debugger_errors_tab_lists_and_navigates`)
- [x] `bottom-monitors` Implement the performance Monitors panel graphing FPS, memory, draw calls, and physics over time during a play session. (test: `bottom_monitors_plot_metrics`)
- [x] `bottom-audio-buses` Implement the Audio bus layout editor (add/remove bus, volume sliders, mute/solo/bypass, effect chain, send routing) with save/load of the layout resource. (test: `bottom_audio_buses_edit_and_persist`)
- [x] `bottom-shader-editor-shell` Implement the bottom Shader editor shell that opens when a shader resource is edited, with tabbed open shaders and a compile/error status line. (test: `bottom_shader_editor_shell_opens_and_reports`)
- [x] `bottom-debugger-profiler` Implement the Debugger profiler tab measuring per-frame function time with start/stop capture and a sortable cost breakdown. (test: `bottom_debugger_profiler_captures_costs`)
- [x] `script-core-syntax-highlight` GDScript syntax highlighting colors keywords, strings, comments, numbers, and types (test: `script_core_syntax_highlight`)
- [x] `script-core-autocomplete` Context-aware autocompletion suggests identifiers, members, and keywords and inserts the chosen entry (test: `script_core_autocomplete`)
- [x] `script-core-auto-indent` Pressing Enter auto-indents the new line based on the previous line and block openers (test: `script_core_auto_indent`)
- [x] `script-core-bracket-autoclose-match` Brackets/quotes auto-close and matching pairs are highlighted (test: `script_core_bracket_autoclose_match`)
- [x] `script-core-comment-toggle` Toggle-comment comments or uncomments the selected lines (test: `script_core_comment_toggle`)
- [x] `script-core-line-operations` Duplicate-line, move-line-up/down, and delete-line operate on the caret line or selection (test: `script_core_line_operations`)
- [x] `script-core-indent-unindent` Tab/Shift+Tab indent and unindent the selected lines by one level (test: `script_core_indent_unindent`)
- [x] `script-core-code-folding` Foldable regions (functions, indented blocks) can be collapsed and expanded (test: `script_core_code_folding`)
- [x] `script-core-multi-caret` Multiple carets / column selection apply edits at all caret positions simultaneously (test: `script_core_multi_caret`)
- [x] `script-core-save-and-trim` Saving writes the buffer and normalizes trailing whitespace / final newline per editor settings (test: `script_core_save_and_trim`)
- [x] `script-nav-find-replace` In-file find and replace supports case/whole-word/regex options and replace-all (test: `script_nav_find_replace`)
- [x] `script-nav-find-in-files` Find-in-files searches across project scripts and lists results with navigation (test: `script_nav_find_in_files`)
- [x] `script-nav-goto-line` Go-to-line jumps the caret to a requested line number (test: `script_nav_goto_line`)
- [x] `script-nav-function-list` A function/member list (outline) lets the user jump to a symbol in the current script (test: `script_nav_function_list`)
- [x] `script-nav-goto-definition` Goto-definition (Ctrl+click / shortcut) navigates to the definition of the symbol under the caret (test: `script_nav_goto_definition`)
- [x] `script-nav-bookmarks` Bookmarks can be toggled on lines and navigated next/previous (test: `script_nav_bookmarks`)
- [x] `script-nav-breakpoints-debugger` Breakpoints can be toggled and integrate with the debugger to pause execution (test: `script_nav_breakpoints_debugger`)
- [x] `script-nav-open-scripts-panel` The open-scripts panel lists loaded scripts and switches the active script on selection (test: `script_nav_open_scripts_panel`)
- [x] `fs-dock-browser` Implement the FileSystem dock with a directory tree and a file list of the selected folder, navigable and reflecting the project's `res://` filesystem. (test: `fs_dock_browser_lists_directory_contents`)
- [x] `fs-dock-file-ops` Implement file operations (new folder, rename, move, duplicate, delete) via context menu and shortcuts, with dependency-safe path remapping on rename/move. (test: `fs_dock_file_ops_remap_dependencies`)
- [x] `fs-dock-drag-to-scene-tree` Support dragging a scene/resource from the dock onto the Scene tree to instance it (scenes) or assign it (resources). (test: `fs_dock_drag_to_scene_tree_instances`)
- [x] `fs-dock-drag-to-inspector` Support dragging a resource from the dock onto a compatible Inspector property slot to assign it. (test: `fs_dock_drag_to_inspector_assigns_property`)
- [x] `fs-dock-thumbnails` Generate and display thumbnail previews for textures and scene/resource files in the file list, refreshing when the source changes. (test: `fs_dock_thumbnails_render_and_refresh`)
- [x] `fs-dock-search-filter` Implement the dock search/filter box that narrows the file list to name matches across the current scope. (test: `fs_dock_search_filter_narrows_listing`)
- [x] `fs-dock-import-hooks` Wire the import pipeline so importable assets re-import on change and expose import settings, marking stale imports. (test: `fs_dock_import_hooks_reimport_on_change`)
- [x] `fs-dock-favorites-context` Implement favorite directories and the file context menu (open, show in file manager, edit dependencies, view owners). (test: `fs_dock_favorites_and_context_menu`)
- [x] `signals-dock-tree` Render the Signals tab as a tree of the selected node's signals grouped by declaring class, with signal signatures and existing connections nested under each signal. (test: `signals_dock_tree_lists_signals_and_connections`)
- [x] `signals-connect-dialog` Implement the Connect-a-Signal dialog to pick a target node and target method from the scene, creating the connection on confirm. (test: `signals_connect_dialog_creates_connection`)
- [x] `signals-connection-flags-binds` Support the dialog's advanced options — extra bound arguments and the deferred/one-shot connect flags — and persist them on the connection. (test: `signals_connection_flags_and_binds_persist`)
- [x] `signals-disconnect-edit` Support editing and disconnecting an existing connection from the signal tree. (test: `signals_disconnect_and_edit_connection`)
- [x] `signals-navigate-to-method` Implement go-to-method navigation that opens the target script at the connected receiver method. (test: `signals_navigate_to_connected_method`)
- [x] `signals-autocreate-receiver` Auto-generate a receiver method stub in the target node's script when connecting to a method that does not yet exist. (test: `signals_autocreate_receiver_method_stub`)
- [x] `signals-docs-tooltips` Surface signal documentation/descriptions as tooltips and a details area in the signal tree. (test: `signals_docs_tooltips_render`)
- [x] `signals-filter-search` Implement a filter box that narrows the signal tree to name matches. (test: `signals_filter_search_narrows_tree`)
- [x] `anim-player-panel` The AnimationPlayer panel lists animations and supports create/rename/duplicate/delete (test: `anim_player_panel_manage`)
- [x] `anim-timeline-playhead` The timeline shows a time ruler and a draggable playhead with zoom and snap (test: `anim_timeline_playhead`)
- [x] `anim-keyframe-edit` Keyframes can be inserted, selected, moved, and deleted on tracks (test: `anim_keyframe_edit`)
- [x] `anim-track-types` The editor supports the core track types (property/value, transform, method-call, audio, animation) (test: `anim_track_types`)
- [x] `anim-track-add-remove` Tracks can be added (including via inspector keying) and removed (test: `anim_track_add_remove`)
- [x] `anim-bezier-editing` Bezier (value-curve) tracks expose draggable handles for easing (test: `anim_bezier_editing`)
- [x] `anim-interpolation-loop-modes` Per-key interpolation modes and animation loop mode are configurable (test: `anim_interpolation_loop_modes`)
- [x] `anim-onion-skinning` Onion skinning previews neighboring frames around the playhead (test: `anim_onion_skinning`)
- [x] `anim-playback-controls` Playback controls drive play/pause/stop, loop, speed, and autoplay-on-load (test: `anim_playback_controls`)
- [x] `anim-tree-graph` The AnimationTree editor edits state-machine / blend-tree nodes and transitions (test: `anim_tree_graph_edit`)
- [x] `systems-project-settings` Implement the Project Settings dialog (general settings tree, add/override/revert a setting) reading and writing `project.godot`. (test: `systems_project_settings_persist`)
- [x] `systems-input-map` Implement the Project Settings Input Map tab (add/remove actions, bind key/mouse/joypad events, set deadzone) persisted to project settings. (test: `systems_input_map_actions_persist`)
- [x] `systems-autoloads` Implement autoload/singleton management (add, rename, reorder, enable, remove) writing the autoload section of project settings. (test: `systems_autoloads_register_globals`)
- [x] `systems-editor-settings` Implement the Editor Settings dialog covering interface, text editor, and shortcut preferences persisted to the editor settings store. (test: `systems_editor_settings_persist_and_apply`)
- [x] `systems-export-presets` Implement export presets (create/edit/delete a preset, select platform, set resources/features, export to a target). (test: `systems_export_presets_create_and_export`)
- [x] `systems-vcs-integration` Implement version-control integration (repo status, stage/unstage, commit, branch list, diff view) via the VCS interface. (test: `systems_vcs_stage_commit_and_diff`)
- [x] `systems-plugin-management` Implement editor plugin management (enable/disable plugins from Project Settings) loading and unloading the plugin at runtime. (test: `systems_plugin_enable_disable`)
- [x] `systems-variant-coverage` Ensure Variant type coverage across settings/export serialization so every supported Variant type round-trips through the settings and preset stores. (test: `systems_variant_coverage_round_trips`)
